import React from 'react';
import { Divider } from 'react-native-elements';

export default () => (
  <Divider
    style={{
      height: 1,
      width: '100%',
      margin: 10,
      backgroundColor: 'white'
    }}
  />
);
